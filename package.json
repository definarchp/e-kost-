{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "build:analyze": "vite build --analyze",
        "preview": "vite preview",
        "cache:clear": "php artisan view:clear && php artisan cache:clear && php artisan config:clear",
        "optimize": "php artisan optimize:clear && php artisan optimize",
        "production": "npm run build && npm run optimize && npm run cache:clear"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^2.0.0",
        "tailwindcss": "^4.0.0",
        "terser": "^5.49.0",
        "vite": "^7.0.7"
    }
}
